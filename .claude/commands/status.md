Show comprehensive project status.

Check and report:
1. Git status (branch, uncommitted changes, ahead/behind)
2. Type errors (`uv run poe typecheck`)
3. Lint issues (`uv run poe lint`)
4. Test results (`uv run poe test`)
5. Any TODO/FIXME comments in recent changes

Format as a clear summary with action items if needed.
